# KILI (legacy path)

MetaHuman / Kili digital-twin assets have been relocated for the rendering
architecture:

```text
assets/avatars/metahuman/
```

This folder is kept so older scripts/docs that mention `KILI/` still resolve.
New code should use `motion_engine.rendering.assets.METAHUMAN_ROOT`.
